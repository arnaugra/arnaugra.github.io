// import dotenv if you are not using github pages
// import dotenv from "dotenv";
// dotenv.config();

export default {
    siteName: process.env.SITE_NAME || "My Tech {log}",
    siteLang: process.env.SITE_LANG || "en",
    blogDescription: process.env.BLOG_DESCRIPTION || "A blog about web development, programming, and technology.",
};